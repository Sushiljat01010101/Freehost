services:
  - type: web
    name: free-html-hosting
    env: php
    buildCommand: composer install --no-dev
    startCommand: php -S 0.0.0.0:$PORT -t .
    plan: free
    envVars:
      - key: PHP_VERSION
        value: "8.1"